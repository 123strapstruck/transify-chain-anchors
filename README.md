# transify-chain-anchors
Daily external anchors for Transify's transaction hash chain. Each entry records a SHA-256 root hash over all workspace chain tails, published here on a fixed schedule so chain state at a given date can be checked against a record Transify's own database does not control. Machine-readable JSON, one file per day.
